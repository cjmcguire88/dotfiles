# eza theme.yml - Custom icons and colors
extensions:
  tlv:
    {
  filename: { foreground: white, is_bold: false },
      icon: { glyph: , style: { foreground: blue } },
    }
  v:
    {
  filename: { foreground: white, is_bold: false },
      icon: { glyph: , style: { foreground: blue } },
    }
  sv:
    {
  filename: { foreground: white, is_bold: false },
      icon: { glyph: , style: { foreground: blue } },
    }

filenames:
  hyprland.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }

  hyprpaper.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }

  monitors.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }

  hypridle.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }

  hyprlock.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }

  keybinds.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }

  ux.conf:
    filename: { foreground: white, is_bold: true }
    icon: { glyph: , style: { foreground: cyan } }
